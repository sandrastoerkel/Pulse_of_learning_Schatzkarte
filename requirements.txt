# Pulse of Learning - Commercial Edition
# Requirements for Streamlit App

# Core Dependencies
streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.17.0

# Database
# sqlite3 is included in Python standard library (für lokale PISA-Daten)

# Cloud Database (Supabase - PostgreSQL)
supabase>=2.0.0

# JWT fuer JaaS Video-Integration
PyJWT>=2.8.0
cryptography>=41.0.0  # Fuer RS256 JWT-Signierung

# Optional (for extended features)
openpyxl>=3.1.0  # Excel export functionality
